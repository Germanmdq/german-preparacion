'use client';
import {useRef,useState} from 'react';import {IntroVideo} from '@/components/IntroVideo';import {PreparationIntro} from '@/components/PreparationIntro';import {Assessment} from '@/components/Assessment';
type Stage='video'|'intro'|'assessment';export default function Page(){const [stage,setStage]=useState<Stage>('video');const presentationRef=useRef<HTMLAudioElement|null>(null);const enter=()=>{const audio=new Audio('/audio/presentation.mp3');audio.preload='auto';presentationRef.current=audio;void audio.play().catch(()=>{});setStage('intro')};return <div className="app-shell">{stage==='video'&&<IntroVideo onFinish={enter}/>} {stage==='intro'&&<PreparationIntro audio={presentationRef.current} onStart={()=>setStage('assessment')}/>} {stage==='assessment'&&<Assessment/>}</div>}
