export function Progress({current,total}:{current:number;total:number}){return <div className="progress-wrap"><div className="progress-label"><span>Pregunta {current} de {total}</span><span>{Math.round(current/total*100)}%</span></div><div className="progress-track" role="progressbar" aria-valuemin={1} aria-valuemax={total} aria-valuenow={current} aria-label={`Pregunta ${current} de ${total}`}><i style={{width:`${current/total*100}%`}}/></div></div>}
