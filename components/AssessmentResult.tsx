import type { ReturnTypeOfBuildResult } from './types';
import { AudioButton } from './AudioButton';
export function AssessmentResult({result,onRestart,onLogout}:{result:ReturnTypeOfBuildResult;onRestart:()=>void;onLogout:()=>Promise<void>}){const secondary=result.secondaryPattern.replaceAll('_',' ');return <main className="screen result-screen"><button className="session-logout result-logout" onClick={()=>void onLogout()}>Cerrar sesión</button><section className="content-card result-card"><p className="eyebrow">HAY ALGO BASTANTE CLARO EN TUS RESPUESTAS</p><h1>{result.copy.headline}</h1><AudioButton audioSrc={result.copy.audioSrc}/><p className="lead">{result.copy.explanation}</p>{result.evidence.length>0&&<div className="evidence"><span>Lo vimos especialmente cuando elegiste:</span>{result.evidence.map((e,i)=><blockquote key={i}>“{e}”</blockquote>)}</div>}<div className="observation"><h2>Qué empezar a observar</h2><p>{result.copy.observation}</p></div><p className="secondary">También aparece, en segundo plano, el patrón <strong>{secondary}</strong>.</p></section><section className="content-card next-card"><h2>Puedo ayudarte a trabajar esto.</h2><p>Durante los próximos 3 días puedo acompañarte en cuatro momentos de tu día para trabajar específicamente sobre este patrón.</p><div className="future-actions" aria-label="Próximamente"><button disabled>Ya soy parte de Germán Asistente</button><button disabled>Quiero ser parte</button></div><small>Próximamente</small></section><button className="btn-quiet" onClick={onRestart}>Volver a empezar</button></main>}
